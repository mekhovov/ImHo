-if user == current_user 
  =link_to 'Редактировать', edit_user_path(user)%p
  =link_to 'Удалить', user, :confirm => 'Вы уверены?', :method => :delete
%p  
  Имя:
  =user.name
%p 
  email:
  =user.email
%p
  =show_photos user, @view_type
%hr