%b
  -if comment.user_id.nil?
    guest:
  -else
    = image_tag "#{request.ssl? ? "https://secure" : "http://www"}.gravatar.com/avatar/#{Digest::MD5.hexdigest User.find(comment.user_id).email}?s=50", :alt => ""
    =User.find(comment.user_id).email + ":"
  =comment.created_at.strftime('%a, %d %B %Y')
%br
=comment.comment
%p